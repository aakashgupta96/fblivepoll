<div class="col s12 m6 l4 hoverable">
	<div class="card">
    <div class="card-content zero-padding">
      <% if post.poll? %>
        <img src="<%=post.image.url%>" class=" activator post-image">
      <% else %>
        <img src="/videos/frame.png" class=" activator post-image">
      <% end %>
    </div>
    <div class="card-tabs">
      <ul class="tabs">
        <li class="tab"><a class="active" href="#title<%=post.id%>">Title</a></li>
        <li class="tab"><a href="#caption<%=post.id%>">Caption</a></li>
        <li class="tab"><a href="#details<%=post.id%>">Details</a></li>
        <i class="activator material-icons right pointer" style="position: relative; top: 10px; right: 8px;">toc</i>
      </ul>
    </div>
    <div class="card-content grey lighten-4">
      <div id="title<%=post.id%>" class="truncate"><%=post.title.capitalize%></div>
      <div id="caption<%=post.id%>" class="truncate"><%=post.caption.capitalize%></div>
      <div id="details<%=post.id%>" class="truncate"><%= link_to "Click here for all details", show_post_path(post.id) %></div>
    </div>
    <div class="card-reveal relative">
      <span class="card-title grey-text text-darken-4 truncate bolder"><%=post.title.capitalize%><i class="material-icons right">close</i></span>
      <p><%=post.caption.capitalize%></p>
      <div class="absolute" style=" bottom: 10px;">
        <% if post.live %>
      		<%= link_to "End Video", stop_post_path(post.id), data: {confirm: "This will end your live post. Are you sure you want to continue?", method: "post" }, class: "btn btn-less-padding margin-2 bolder"%>
      	<% elsif post.scheduled? %>
      		<%= link_to "Cancel Schedule", cancel_schedule_path(post.id), data: {confirm: "Are you sure you want to cancel this scheduled post?", method: "post" }, class: "btn btn-less-padding margin-2 bolder"%>
      	<% end %>
      	<a href="/posts/<%=post.id%>" class="btn bold dull-purple">Show Full Post</a>
      </div>
    </div>
  </div>
</div>